apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dex.configMapName" . }}
  labels:
{{ include "service-base.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ include "dex.config" . | indent 4 }}
