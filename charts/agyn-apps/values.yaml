# Nothing to configure. The runner and the apps are dependencies of
# agyn-platform now, each behind one flag that both deploys the workload and
# declares it: bundledRunner.enabled, bundledApps.<name>.enabled.
